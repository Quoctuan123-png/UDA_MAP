const sequelize = require("../config/db"); // ✅ Đảm bảo bạn có sequelize từ file config
const { Op, Sequelize } = require("sequelize"); // ✅ Thêm Sequelize
const {  } = require("../models");

const createTienIch = async (req, res) => {

}
exports { createTienIch}